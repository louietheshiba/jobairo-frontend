import React from 'react';

import Container from './Container';

const Header = () => {
  return (
    <header className="z-30 flex h-16 items-center border-b border-gray-50 bg-white md:h-[70px]">
      <Container>Header</Container>
    </header>
  );
};

export default Header;
